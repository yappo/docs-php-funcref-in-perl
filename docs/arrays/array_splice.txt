*array_splice

**PHP

>|PHP|
array array_splice ( array &$input , int $offset [, int $length = 0 [, mixed $replacement ]] )
||<

**Perl

>|Perl|
my @spliced = splice(@array, $offset, $length, @list);
# splice(@array, $offset, $length);
# splice(@array, $offset); 
# splice(@array);
||<
